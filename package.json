{
  "name": "auto-mo-live",
  "private": true,
  "version": "1.1.8",
  "description": "Chrome extension Auto Mở Live + website công khai cho privacy/OAuth verification",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "pack": "node scripts/pack-all.js",
    "fonts": "./scripts/fetch-fonts.sh",
    "deploy": "wrangler deploy"
  },
  "license": "MIT",
  "devDependencies": {
    "adm-zip": "^0.6.1",
    "archiver": "^8.0.0",
    "archiver-cli": "^0.1.0",
    "esbuild": "^0.28.2"
  }
}
